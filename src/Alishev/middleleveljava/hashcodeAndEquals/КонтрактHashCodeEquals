Контракт HashCode() Equals()
Алгоритм действия:
1. У двух проверяемых объектов вызываем метод HashCode(). Это быстро.
если Хеши коды разные -> два объекта точно разные и проверка закончена.
2. Если Хеш значение одинаковое, то тут два варианта
 Либо 2 объекта действительно идентичны
 Либо это хеш-коллизия(когда введу ограниченности числа как такового(int), может случится совпадение хеш значения у двух разных объектов)

2.1 Вызываем  метод Equals(). Он сравнивает объекты по каждому полю. Это долго.

3. Метод Equals() выдает итоговый ответ
------------------------------------------------------

Чем меньше коллизий чем реже вызываем метод Equals() тем быстрее программа!!!