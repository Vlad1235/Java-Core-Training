package Arrays.arrays3;/*
public class MassiveLenghFind {
    /*
    Размер массива не всегда очевиден, поэтому для того, чтобы его узнать следует использовать свойство length,
    которое возвращает длину массива.

Пример: Заданы произвольные 4 числа(элемента), необходимо найти минимальное

    public static void main (String[] args){
        int [] myArray = {12, -13, 0, 15};
        int min = myArray[0];

        for(int i =0; i < myArray.length; i++ ){
            if(min>myArray[i]){
                min = myArray[i];
            }
        }
        System.out.println(min);
    }
}

*/